## UI components (JabKit)

This project uses JabKit. Search the JabKit catalogue before hand-writing a reusable component. Components live in `src/components/jabkit/`. Use semantic token classes such as `bg-background`, `text-foreground`, and `border-border`, never hardcoded colors.
