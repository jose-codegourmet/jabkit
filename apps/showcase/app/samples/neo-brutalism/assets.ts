import type { SampleAssetMap } from "../../../lib/design-system-assets";

/** Filled by NEO-03. Empty until Higgsfield series production. */
export const assets = {} as const satisfies SampleAssetMap;
